<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container">

    <header>
        <% include ../partials/header %>
    </header>

    <main>
        <div class="jumbotron">
            <h1>List of URLs</h1>
            <ul>
                <% for (urlDatabase in urls) { %>
                    <li><%= `${urlDatabase} - ${urls[urlDatabase]}`  %></li>
                <% }; %>
            </ul>
        </div>
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>

</body>
</html>